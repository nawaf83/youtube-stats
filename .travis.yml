language: node_js
os: windows-latest
install:
  - yarn
 node_js:
  - 16.13.0

services:
  - docker

before_install:
  - docker build -t nawaf83/reactapp -f Dockerfile.dev .
  
script:
  - docker run -e CI=true nawaf83/reactapp npm run test
